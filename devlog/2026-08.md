# Dev Log — August 2026

---

## 2026-08-02: Person settings, share chips with prefill, people carousel, sign in keep or leave question (iOS)

### What changed
- `PeopleStore.swift`: `Person` gains optional `closeness`, `rhythm`, `depthPreference` (explicit, set only on the settings sheet) and `sharedCloseness` (derived hint from shares); setters, `effectiveCloseness` (explicit first, hint second), `pruneOrphans()`; one orphan sweep per launch, deferred a cycle.
- `SpacePersonSettingsView.swift`: new About section with three segmented pickers (closeness, rhythm, depth), unselected by default, save on tap, per row Clear with accessibility labels; spacing matched to Natural Reach and MoreView rhythm.
- Rhythm keys renamed before first release: `every_week`, `every_month`, `now_and_then`, so keys match the shipped labels with no migration ever needed.
- `NaturalReachView.swift`: person chip row (recent six) above the pickers; tapping prefills closeness from the person and recency derived from send timestamps (ongoing within 14 days, few weeks within 90); sharing with a chip selected attaches the person silently, skips the who prompt, bumps recency, writes the share closeness back as hint; new `preselectedPersonID` input for a later person page entry.
- `SpaceSentInfoSheet.swift`: a send without a person now renders ("No name yet") with Delete from history in its menu; Sent section shows whenever any send exists, so unnamed sends are never stuck.
- `SharedQuestionsStore.swift`: `detachPerson`, `removeShare`, `removeShares` end with `pruneOrphans()`, so a person with zero sends disappears from chips, prompts and sheets immediately.
- `CloudSync.swift`: sign in refactored around a three case rule; keep or leave question fires only when the account has content AND the device holds activity from another owner; new `localDataOwner` marker tells account residue apart from guest activity, so relaunches stay silent; sign out now pushes and clears all five stores (was play stats only); `applyRemote`, `pushAll`, `clearAllLocal` helpers; orphan prune after every pull.
- `RootView.swift`: keep or leave alert; Keep bold as default, no destructive role (which had auto inserted a dead Cancel).
- `MyQwavaView.swift`: Your people chip carousel below the deck suggestion, shown from two people, question card background plus `spaceCardShadow`, navigates to the person pages; first direct door to them.
- Copy passes applied from the Copy chat: person settings About section, Natural Reach chip row and subtitle, merge alert.

### Key decisions
- Explicit person settings are the user's word: shares update the derived hint only, never the sheet values.
- Rhythm (pattern) never feeds the recency prefill (moment); recency reads only from real send timestamps.
- Orphan rule: no sends, no person, settings included; a person exists in Qwava through the questions sent to them.
- Merge question only in the genuinely ambiguous case; guest to first account migration stays silent and untouched.
- Follow ups feature scoped in discussion: per deck plus universal plus later tiers in one `follow_ups.json`, sent follow ups recorded as sends, shared direct without the Natural Reach panel; content being written in the Decks chat.

### Files changed
- `PeopleStore.swift`, `SpacePersonSettingsView.swift`, `NaturalReachView.swift`, `SpaceSentInfoSheet.swift`, `SharedQuestionsStore.swift`, `CloudSync.swift`, `RootView.swift`, `MyQwavaView.swift`

### Unresolved
- Follow up questions content in progress (Decks chat); the in app mechanic is the next build once the file exists.
- Placeholder copy pending: "No name yet" row in the sent sheet, "Your people" carousel title.
- Recency prefill thresholds (14 and 90 days) work but await a conscious yes.
- People carousel: position may move, See all page later, shows only from two people so a single saved person still lacks a direct door.
- `preselectedPersonID` has no caller yet; waits for person page suggestions or follow ups.

## 2026-08-04: Search question zoom transition, people chip circles, daily question subline (iOS)

### What changed
- `HomeSearchOverlay.swift`: own `@Namespace`; the expanded question cover gains `.zoomDestination(id:in:)`, mirroring the Your Space pairing in `MainTabsView`.
- `HomeSearchResults.swift`: `HomeSearchResultsList` takes a `zoomNS`; question results carry `.zoomSource(id: item.id, in: zoomNS)`, so a result grows open and drags down to close.
- `SpaceCompactCards.swift`: `SpaceCompactQuestionCard` gains `castsShadow` (default true); search passes false and applies `.spaceCardShadow(opacity: 0.12)` outside the source.
- `MyQwavaView.swift`: new `SpacePersonChip` and `SpacePersonChipHeightKey`; circle sized from the measured chip height less 8, filled with the deck's `cardBackgroundColor`, 1pt `strokeBorder` in `cardOutlineColor`, initial in `titleColor`.
- `MyQwavaView.swift`: new `lastSentDeckCard(for:)`; `shares` is stored newest first, so the first match is the latest send.
- `MyQwavaView.swift`: Today's question header split into a `VStack` at spacing 4, subline "The same for everyone, every day." in callout `SublineInk`, `See all` moved onto the subline row.
- `MyQwavaView.swift`: Your people top padding 32 to 40, matching Still to explore; placeholder title comment removed.
- `SpaceSentInfoSheet.swift`: `"No name yet"` to `"No name"`, `"Touch and hold a name for options."` to `"Touch and hold for options."`; header comment rewritten, dropping the stale placeholder note and the outdated Sent section rule.

### Key decisions
- Shadow lag on dismiss is a location problem, not a drawing one; same fix as 4 July, now applied to the compact card because it became a zoom source.
- Initial takes the deck's `titleColor` rather than a hardcoded white; all 50 decks are `#ffffff` today, so it survives a deck changing.
- Circle measured from the chip height, safe because it is an overlay and takes no part in layout, so it cannot drive the height it reads.
- Subline at callout rather than `See all`'s body size: 17pt wraps on every phone, and callout matches the existing `SheetSectionHeader` pattern.
- `lastTextBaseline` on the subline row so `See all` follows the sentence when it wraps at large Dynamic Type.
- `"No names on this one yet."` kept as the empty state: "yet" points forward at the field below it, the helpful direction, where on a row label it pointed backward at something undone.

### Files changed
- `HomeSearchOverlay.swift`, `HomeSearchResults.swift`, `SpaceCompactCards.swift`, `MyQwavaView.swift`, `SpaceSentInfoSheet.swift`

### Unresolved
- Person chip circle sizes from a measured height, so it is zero wide on the first frame; check the row does not jump on device.
- Your people spacing at 40 may still read tight, since `spaceCardShadow` paints below the card above it; 48 is the next step.
- Android parity for all of this.

## 2026-08-07: Follow up layer, people merge fix, person page polish (iOS)

### What changed
- New `FollowUpsStore.swift` (Data): `FollowUpItem` model, bundle load plus CDN refresh with disk cache, tier selection, per person offer state in UserDefaults (`FollowUpOffers.v1`), cleared on `.qwavaResetAll`.
- New `SpaceFollowUpCard.swift` (Screens/MyQwava): offer card at the top of the person page; editable `TextEditor`, `Try another` cycles tiers until edited, `Share` opens its own `FollowUpShareSheet` and records the send on completion; `No thanks` marks the offer handled.
- `RemoteContent.swift`: optional `follow_ups_url` manifest key, `loadFollowUpsData(from:)` with fixed CDN path fallback and cache.
- `QwavaApp.swift`: follow ups bundle load at launch, remote refresh alongside questions.
- `SpaceSentView.swift`: card inserted above the groups; page dismisses itself via `personMissing` watcher when its person is removed; page wide tap resigns the keyboard.
- `SpaceDetailScaffold.swift`: `.scrollDismissesKeyboard(.interactively)` on the scroll view.
- `SpaceSentStub.swift`: `spaceCardShadow()` applied after the context menu; header comment updated to reviewed.
- `PeopleStore.swift`: `mergeRemote` folds same name people into the most recently used, reassigning sends via `reassignPerson(from:to:)`.
- `NaturalReachView.swift`: `shareAppName(for:)` made internal for reuse; `ShareSheet` stays private after a redeclaration clash with `QuestionPlayerView`.
- Copy applied from review: subline "A follow up to the last question you sent.", `Suggested`, `Try another`, `Share`, `No thanks`; title "Keep it going?" approved as drafted.

### Key decisions
- Default tier from recency: question specific under 14 days, later tier beyond, matching the chip threshold; no offer past 30 days so the card never outlives the page window.
- Cycle first, edit last: an edit hides `Try another` rather than risking a silent overwrite.
- One card per person, not per sent question, so the offer stays an offer instead of wallpaper.
- No offer on a follow up (`fu_` prefix guard), stopping the chain cleanly.
- Share sheet over a WhatsApp deep link: a deep link has no completion callback, and honest recording beats one saved tap.
- Not now dismisses silently for the current send with no reminder; reminders belong to the nudge, still parked.
- Keyboard: page tap, card tap and drag dismiss; keyboard bar `Done` built then removed, the floating capsule reads as a stray button.
- Recency prefill thresholds (14 and 90) blessed as final; the 30 day offer window blessed with them.
- Sync duplicate people are a merge bug, not a UX gap: names identify people everywhere else, so `mergeRemote` enforces it too; survivor keeps settings, absorbed settings accepted as lost.
- Stub shadow applied outside the context menu so the lifted preview stays clean.

### Files changed
- New: `FollowUpsStore.swift`, `SpaceFollowUpCard.swift`, `follow_ups.json` (bundle)
- `RemoteContent.swift`, `QwavaApp.swift`, `SpaceSentView.swift`, `SpaceDetailScaffold.swift`, `SpaceSentStub.swift`, `PeopleStore.swift`, `NaturalReachView.swift`

### Unresolved
- Offer dismissals are local only; a dismissal on one device can reappear on another. Accepted for v1.
- Absorbed person settings are dropped when the merge folds duplicates; rare once duplicates stop forming.
- Editability of the suggestion is not signposted; watch whether testers treat the field as read only.
- "Share" next to "No thanks" is five characters against eight on equal capsules; check the pair reads right.
- Three near identical share sheet wrappers now exist; collapse into DesignSystem in a refactor session.
- Android parity, riding with the Connection Rhythm parity work.

## 2026-08-07: Connection Rhythm v1, follow ups, Home search (Android)

**What changed**
- Schema contract landed: `SharedQuestionsStore.kt` rewritten (row key question plus person, cap 300, optional `personId` and `appUsed`), new `PeopleStore.kt`, `CloudSync.kt` additive people section, orphan prune after every pull.
- Recording moved to chooser app selection via manifest declared `ShareResultReceiver`; payload rides PendingIntent extras; friendly app names mapped from package fragments.
- Post share who prompt as a Compose dialog (`SharePersonPrompt.kt`); `ConnectionRhythmSettings` holds the ask who switch.
- Sent stubs on Your Space (grouped by question and day, cap 3) and See all (uncapped, clock time, grouped within day); window 14 to 30 days; footer copy updated.
- Person pages via `SpaceSentScreen` person mode; `SpacePersonSettingsScreen.kt` with rename, merge on collision, About pickers, delete with sent count; page self closes when its person is removed.
- Share chips on Natural Reach prefill closeness and recency and attach silently, skipping the prompt; `noteSharedCloseness` hint written on chip shares only.
- People carousel on Your Space; chip circles carry the last sent deck's colours.
- Sign in keep or leave: three case rule, `localDataOwner` marker, `pendingMerge` alert in `RootView.kt`; sign out flushes all five stores while still authenticated, wipe guarded on flush success.
- Sent info sheet (`SpaceSentInfoSheet.kt`, ModalBottomSheet) behind three dots on sent question details; `PendingQuestionDetail.sentQuestionId` consumed on read so stale context never lights the dots.
- Follow up card on person pages (`SpaceFollowUpCard.kt`, `FollowUpsStore.kt`); `follow_ups.json` in assets plus CDN via `follow_ups_url` with cache fallback.
- Riders: pull to refresh on Your Space, Today's question subline row with See all, See all rename on all section links, seeded Home shuffle.
- Home search: icon left of shuffle, full page `SearchScreen.kt` with `HomeSearchEngine`, compact result cards in `SpaceCompactCards.kt`.

**Key decisions**
- Record on app selection (IntentSender): the closest honest signal Android offers; a cancelled chooser records nothing, backing out inside the chosen app still records.
- Copy chip records nothing on Android (no chosen component callback) while iOS records "Copy"; accepted platform gap.
- Firestore key is `personID` with capital ID, matching shipped iOS code; docs said `personId`, code wins.
- Prompt and sheets take Android native shapes: dialog for the who prompt, ModalBottomSheet without a Done button, DropdownMenu for context menus.
- Sign out flushes before `Auth.signOut()` (`flushBeforeSignOut`): pushes after signout are unauthenticated, Firestore rejects them, and the wipe guard blocked the wipe (found on device).
- Home shuffle seeded by the saved nonce: navigation return rebuilt `remember` and reshuffled; same user facing bug as iOS with a different trigger.
- Search query in `rememberSaveable` for the same navigation trap; screen state that must survive visiting another screen needs saveable, not remember.
- Gson with a TypeToken needs the explicit type argument when the result feeds an elvis; inferred `Nothing` broke `follow_ups.json` parsing at runtime.
- Search is a full page with the field on top (Android convention), not the iOS pull gesture; deck category index uses raw keys.

**Files changed**
- New: `PeopleStore.kt`, `ShareResultReceiver.kt`, `ConnectionRhythmSettings.kt`, `SharePersonPrompt.kt`, `SpaceSentStub.kt`, `SpacePersonSettingsScreen.kt`, `SpaceSentInfoSheet.kt`, `FollowUpsStore.kt`, `SpaceFollowUpCard.kt`, `SearchScreen.kt`, `SpaceCompactCards.kt`, `follow_ups.json`.
- Rewritten: `SharedQuestionsStore.kt`, `CloudSync.kt`, `SpaceSentScreen.kt`, `MyQwavaScreen.kt`.
- Edited: `NaturalReachScreen.kt`, `MainNavigation.kt`, `RootView.kt`, `MoreScreen.kt`, `SpaceQuestionDetailScreen.kt`, `SpaceCards.kt`, `HomeScreen.kt`, `PersonalizationEngine.kt`, `RemoteContent.kt`, `QwavaApplication.kt`, `PendingQuestionDetail.kt`, `AndroidManifest.xml`.

**Unresolved**
- Ask who off switch row not yet in the Your Space settings screen; the setting defaults to on.
- Play data safety form must be reopened before release: people names, per person settings, and app used sync for account users.
- Finding for iOS: its sign out pushes also run after `Auth.signOut()` and only win the token teardown race; the same pre flush is recommended there.
- Three segmented control recipes now exist (Quick Mix, Natural Reach, person settings); folded into the DesignSystem refactor task.
## 2026-08-08: Follow ups on person pages, follow up lifecycle, deck page tapering, search icon (iOS)

### What changed
- `SharedQuestionsStore.swift`: `isFollowUp` (`fu_` prefix) and stored optional `anchorQuestionID` on `SharedQuestion`; `record` takes and stores the anchor; `removeShare` also removes the row's follow ups; `detachPerson` also detaches them.
- `CloudSync.swift`: `anchorQuestionID` in the sent questions push and pull, optional like `appUsed`.
- `SpaceFollowUpCard.swift`: send recorded with `anchorQuestionID`; card shows the anchor question quoted in secondary grey under the subline; subline now ends in a colon (placeholder for the Copy chat); header block bottom padding 4; editor `minHeight` 96 to 80.
- `MyQwavaView.swift`, `SpaceSentView.swift`: follow ups filtered out of Sent out lately and See all group building.
- `SpaceSentView.swift`: person pages render each anchor's follow ups beneath its stub as `SpaceCompactQuestionCard` (no mini deck) plus own stub with delete and detach; oldest first; `followUps(for:)` helper.
- `String+Typography.swift`: function word runs glue only their last member; words already holding glue are never glued onto; new `balancedTail` (max 5 words, 24 chars, 55% of text) applied to follow up card text.
- `SpaceDecksView.swift`: `usesCompactCard` widened to `aFew` on the play buckets; recency buckets gained the same, small from `twoToFour`; recent render branches on it.
- `SpaceDetailScaffold.swift`: optional `scrollTopTrigger`, top anchor at file scope (generic type cannot hold a static stored property), instant snap on change; decks page passes its sort.
- `MainTabsView.swift`: magnifier button left of shuffle opens search via new `HomeSearchModel.open()` (same commit spring as the pull); title "Qwava Sets" to "Qwava"; count line "All {total} decks" / "{shown} of {total} decks".

### Key decisions
- A follow up only exists in combination with its question: hidden standalone, cascaded on anchor delete and unname; a detached follow up is invisible for good, accepted.
- Anchor link stored per row (`anchorQuestionID`) because universal and later tiers cannot be traced through content; pre-existing test records lack it and never cascade.
- Follow up card carries its anchor text so the card is self contained, ready for placement off the person page in v2.
- Glue chains capped at one pair so no chunk welds three or more words; a line may now end on the first word of a run, chosen over oversized chunks.
- Scroll snap on sort change is instant, not animated, since the content reorders in the same frame.
- Search icon added as the visible door; the pull gesture stays as the fast path.

### Files changed
- `SharedQuestionsStore.swift`, `CloudSync.swift`, `SpaceFollowUpCard.swift`, `MyQwavaView.swift`, `SpaceSentView.swift`, `String+Typography.swift`, `SpaceDecksView.swift`, `SpaceDetailScaffold.swift`, `MainTabsView.swift`, `HomeSearchOverlay.swift`

### Unresolved
- Follow up card subline colon and the person page follow up display are pending a Copy and Decks look respectively.
- Follow up card tap dips but does nothing (no expand wired); decide expand or inert.
- A follow up outlives its anchor's 30 day window invisibly when the anchor ages out; stored, not shown.
- "Sets" as a possible brand term in App Store copy, website, or pitch deck to be confirmed before release.
- Android parity for the whole session; briefing handed to the Android chat.
