# September 2026

## 2026-09-04 (Translation 03, iOS)

**What changed**
- Full content export from the sheets: EN set rebuilt plus complete DE set, uploaded to CDN `v1/` and `v1/de/` with a language manifest each
- EN rebuild fixed stale `deck_name` in `questions.json`/`deck_filters.json` (8 decks, 485 rows), curly apostrophes on six decks, `CRecent` typo, `Week Kickoff` label
- German content added to the app bundle; `Localizable.xcstrings` created via compiler extraction and filled (249 keys, 223 DE, 26 marked non translatable)
- Person chip labels single sourced into `PersonOptionCatalog` in `PersonPromptEngine.swift`; `SpacePersonSettingsView` reads it; interests why line unified to "Points Qwava toward decks you would both pick."
- Code carried strings converted to `String(localized:)`: person page, `Filters.swift` labels, Your Space headlines, count words, month names

**Key decisions**
- Content per language via manifest: `v1/de/manifest.json` points at DE files, shared `deck_filters` and images (future language = one CDN folder)
- String Catalog over classic `.lproj` (visual coverage for review)
- Legacy `DeckTag` enum and `selectedTags` deleted from `Filters.swift` (zero references)

**Files changed**
- `decks/questions/follow_ups/messages/deck_labels/decksfav/deck_filters.json` (EN + DE), `manifest.json` + `de/manifest.json`, `Localizable.xcstrings`, `PersonPromptEngine.swift`, `SpacePersonSettingsView.swift`, `Filters.swift`, `MyQwavaView.swift`, `SpaceDecksView.swift`, `SpaceSentView.swift`, `SpaceDailyArchiveView.swift`, small edits across a dozen views

**Unresolved**
- DE proofreading pass on device

## 2026-09-05 (Translation 03, iOS)

**What changed**
- `AppLanguage.swift` (new): stored pick with phone fallback, language routed bundle/CDN/caches, string table override for instant switch, root rebuild via `LanguageAwareRoot`
- Language row in More above lock screen questions; switching reloads all content stores per language
- Bundled DE ships as flat `de_` files at bundle root (project uses synchronized folders, which rejected blue folder references; caused duplicate resource collisions until switched)
- Home widget follows the pick via App Group `group.com.qwava.shared`; `de_questions.json` in widget bundle; `WidgetCenter` reload on switch
- Watch app + Smart Stack follow watch system language; watch app streams `v1/de/questions.json` from CDN (it never bundled questions, feature reference corrected)
- Table mode buttons localized by passing labels through `TableModeAttributes` (chose `Weiter`/`Überspringen`)

**Key decisions**
- Watch does not sync the in app pick; follows its own language (WatchConnectivity sync deferred)
- German naming set in catalog: `Dein Space`, `Schnellmix`, `Bleibt bei mir`, `Extras`, `Frage des Tages`

**Files changed**
- `AppLanguage.swift` (new), `RemoteContent.swift`, `DeckLabelsStore.swift`, `MessageLibraryStore.swift`, `QuestionsView.swift`, `MoreView.swift`, `QwavaApp.swift`, `MixView.swift`, `FiltersView.swift`, `QuestionPlayerView.swift`, `NaturalReachView.swift`, `DailyQuestion.swift`, `TableModeAttributes/Session/LiveActivity`, `QwavaWidget.swift`, `WatchStore/SettingsView/QuestionView`, `QwavaWatchWidget.swift`, `Localizable.xcstrings` (grew to 399 keys)

**Unresolved**
- DE proofreading on device (own chat); Live Activity intent titles still EN; Android needs the whole German layer


## 2026-09-06 — Android German layer complete (Translation 04, Android)

**What changed**
- `values/strings.xml` and `values-de/strings.xml` generated from the iOS catalog via `docs/convert.py`; key to resource mapping in `docs/key_mapping.json`
- Six German content JSONs bundled under `assets/de/`
- `AppLanguage.kt` answers manifest URL, asset path, cache suffix and current language; `ContentLoader.kt` is the one door for bundled load and CDN refresh
- Full string sweep: every screen and engine reads resources; option labels became res IDs on `PersonPromptField`, the filter enums, sort modes and Quick Mix
- Language row in More via `AppCompatDelegate.setApplicationLocales`; `MainActivity` moved to `AppCompatActivity`, theme to `Theme.AppCompat`, `autoStoreLocales` service in the manifest
- Sent day labels and time phrases format in the app language, replacing the pinned `Locale.US` and `Locale.getDefault()`
- Card back tap opens the deck like iOS; spacing fixes on More, Quick Mix and the card back detail rows

**Key decisions**
- App locales are the single language truth, so the in app pick and Android 13 per app settings cannot drift
- Card backs adopt the iOS wording set (`Just two of us` family) for one voice across platforms
- Android only strings live in `strings_android.xml`, apart from the generated file so regeneration never eats them
- Deliberate English: messenger names, brand, version footer; `DeckTag`, `Vibe` and `RemoteContentManager` flagged dead

**Files changed**
- ~45 Kotlin files across `ui/screens`, `data`, `models`; `themes.xml`, `AndroidManifest.xml`, `build.gradle.kts`, both string file pairs, `assets/de/`

**Unresolved**
- German Play Store listing (no code)
- Dead code cleanup: `RemoteContentManager`, `DeckTag`, `Vibe`, duplicate filter label maps

## 2026-09-08 (Development 31, iOS)

**What changed**
- Launch red shift fixed: new `LaunchBackground` colorset, `LaunchScreen.storyboard` rebound to it (old `PrimaryColor` name collided with the generated `.primary` symbol)
- Player resolves deck titles live by `deckKey` via `liveDeckTitle(for:)` in `QuestionPlayerView.swift`
- Depth sheet greys and disables levels with no questions; session defaults and Reset trim to available depths
- Dead `colorScheme` properties removed from `SheetCard` and `PersonSettingsCard`
- Language pick now syncs `AppleLanguages` (on select, plus a one time bootstrap sync); no pick fallback uses `Bundle.preferredLocalizations` matching in app and widget
- Table mode lock screen labels resolve through `NSLocalizedString` (instant with the pick); language row value restyled to match the other More actions

**Key decisions**
- `deck_name` column stays in the JSONs: every shipped build requires it to decode, so removal waits until old versions die out
- Completed screen bookmark chip stays hard white by design (deck colours and artwork don't adapt to dark mode)
- Live Activity intent titles stay English: `isDiscoverable: false`, never user visible

**Files changed**
- `LaunchScreen.storyboard`, `Assets.xcassets` (`LaunchBackground`), `QuestionPlayerView.swift`, `SpaceSentInfoSheet.swift`, `SpacePersonSettingsView.swift`, `AppLanguage.swift`, `QwavaWidget.swift`, `TableModeSession.swift`

**Unresolved**
- 296 `String(localized:)` call sites lag a language pick until a cold launch; per language bundle sweep proposed, pending an external review, blocked on the day's push
- Depth library audit: all 55 decks now cover all three depths, so the old "empty levels" report can't reproduce on real decks (visible on The Keepers)
## 2026-09-09 (iOS)

**What changed**
- Language sweep shipped and verified: all 293 `String(localized:)` sites resolve through `AppLanguage.stringsBundle` + `currentLocale`; three `SpacePlaceholders` statics became computed so placeholders switch live
- `PersonSectionHeader`: Clear moved to the subline row, bottom aligned like See all
- Person page prompt slot wrapped in `SpaceCardGrid`; slot cards take the sent cards' column width on iPad
- Slot now renders on aged out person pages; empty history shows a centered two line window explanation (new key, EN + DE)
- Card back detail spacing matched to Android: 16 between groups, 4 label to value (`CardCarouselView`)

**Key decisions**
- Sweep via one Xcode regex instead of 29 pasted files (paste risk; compiler nets bad edits)
- Explicit `bundle:`/`locale:` args over a wrapper (a wrapper breaks catalog extraction)
- Generic empty line survives only on the main sent page (only reachable by deleting the last send live; the window pair would be wrong there)

**Files changed**
- `AppLanguage.swift`, `SpacePlaceholders.swift`, 27 sweep files, `SpacePersonSettingsView.swift`, `SpaceSentView.swift`, `CardCarouselView.swift`, `Localizable.xcstrings`

**Unresolved**
- Android twin: person page empty state fix + the new string pair

## 2026-09-10 (iOS)

**What changed**
- Saved questions sync gains the iOS tombstone half: `savedAt` and `removed` maps beside `byDeck`, merge resolves per id by newest timestamp, push switched to `mergeFields` so shrinking maps land (`SavedQuestionsStore.swift`, `CloudSync.swift`)
- `reset()` tombstones every saved id so Clear propagates; sign out local wipe leaves none
- Local save carries all three maps in one `PersistedState`, silent migration from the legacy bare map
- Pull no longer aborts on a missing `byDeck`; tombstones can arrive alone

**Key decisions**
- Schema and merge mirror Android `0a3c1101` exactly, read from the shipped Kotlin side by side, not from the briefing alone
- Stamp only changes save explicitly, since the autosave watches `byDeck` alone

**Files changed**
- `SavedQuestionsStore.swift`, `CloudSync.swift`

**Unresolved**
- Favorites, sent questions and people still union merge with the same resurrection risk; adopt the tombstone shape once saved questions prove out cross device

## 2026-09-11 (Android 19)

**What changed**
- Saved questions sync gains tombstones: `savedAt` and `removed` maps inside `savedQuestions`, merge resolves per id by newest timestamp, push switched to `mergeFields` so shrinking maps actually land (`SavedQuestionsStore.kt`, `CloudSync.kt`)
- `reset()` tombstones every saved id so a clear propagates; sign out local wipe leaves none
- Local save carries all three maps, silent migration from the legacy bare map
- Depth filter enabled on The Keepers (`FavoritesScreen.kt`); levels with no questions greyed and disabled, session defaults and Reset trim to available depths (`DepthFilterScreen.kt`, `QuestionPlayerScreen.kt`)
- Person page: prompt slot renders on aged out pages, empty history shows the centered two line window note (new key `what_you_sent_is_older_than_that`, EN + DE); all 30 day lines to 0.8 alpha
- Player resolves deck titles live by `deckKey` via `liveDeckTitle()`, including the title recorded on a send
- Settings order matched to iOS: language first, vibrations below it with its description line (`MoreScreen.kt`)

**Key decisions**
- Tombstones over the union merge: a pure union could never delete, so any device still holding an item resurrected it on its next pull and push
- Same schema shipped on iOS the same day; both platforms read and write `savedAt` and `removed`
- Favorites and sent keep the union merge for now; tombstone shape designed to fit them later
- Prompt slot swap animation dropped, effort against value

**Files changed**
- `SavedQuestionsStore.kt`, `CloudSync.kt`, `FavoritesScreen.kt`, `DepthFilterScreen.kt`, `QuestionPlayerScreen.kt`, `SpaceSentScreen.kt`, `MoreScreen.kt`, `values/strings.xml`, `values-de/strings.xml`

**Unresolved**
- Dead code audit found `RemoteContentManager.kt` and the `this_removes_all_bookmarked_decks` string unreferenced, deletion pending
- `values/strings.xml` was hand edited this session; regenerate via `docs/convert.py` to restamp it

