# September 2026

## 2026-09-04 (Translation 03, iOS)

**What changed**
- Full content export from the sheets: EN set rebuilt plus complete DE set, uploaded to CDN `v1/` and `v1/de/` with a language manifest each
- EN rebuild fixed stale `deck_name` in `questions.json`/`deck_filters.json` (8 decks, 485 rows), curly apostrophes on six decks, `CRecent` typo, `Week Kickoff` label
- German content added to the app bundle; `Localizable.xcstrings` created via compiler extraction and filled (249 keys, 223 DE, 26 marked non translatable)
- Person chip labels single sourced into `PersonOptionCatalog` in `PersonPromptEngine.swift`; `SpacePersonSettingsView` reads it; interests why line unified to "Points Qwava toward decks you would both pick."
- Code carried strings converted to `String(localized:)`: person page, `Filters.swift` labels, Your Space headlines, count words, month names

**Key decisions**
- Content per language via manifest: `v1/de/manifest.json` points at DE files, shared `deck_filters` and images (future language = one CDN folder)
- String Catalog over classic `.lproj` (visual coverage for review)
- Legacy `DeckTag` enum and `selectedTags` deleted from `Filters.swift` (zero references)

**Files changed**
- `decks/questions/follow_ups/messages/deck_labels/decksfav/deck_filters.json` (EN + DE), `manifest.json` + `de/manifest.json`, `Localizable.xcstrings`, `PersonPromptEngine.swift`, `SpacePersonSettingsView.swift`, `Filters.swift`, `MyQwavaView.swift`, `SpaceDecksView.swift`, `SpaceSentView.swift`, `SpaceDailyArchiveView.swift`, small edits across a dozen views

**Unresolved**
- DE proofreading pass on device

## 2026-09-05 (Translation 03, iOS)

**What changed**
- `AppLanguage.swift` (new): stored pick with phone fallback, language routed bundle/CDN/caches, string table override for instant switch, root rebuild via `LanguageAwareRoot`
- Language row in More above lock screen questions; switching reloads all content stores per language
- Bundled DE ships as flat `de_` files at bundle root (project uses synchronized folders, which rejected blue folder references; caused duplicate resource collisions until switched)
- Home widget follows the pick via App Group `group.com.qwava.shared`; `de_questions.json` in widget bundle; `WidgetCenter` reload on switch
- Watch app + Smart Stack follow watch system language; watch app streams `v1/de/questions.json` from CDN (it never bundled questions, feature reference corrected)
- Table mode buttons localized by passing labels through `TableModeAttributes` (chose `Weiter`/`Überspringen`)

**Key decisions**
- Watch does not sync the in app pick; follows its own language (WatchConnectivity sync deferred)
- German naming set in catalog: `Dein Space`, `Schnellmix`, `Bleibt bei mir`, `Extras`, `Frage des Tages`

**Files changed**
- `AppLanguage.swift` (new), `RemoteContent.swift`, `DeckLabelsStore.swift`, `MessageLibraryStore.swift`, `QuestionsView.swift`, `MoreView.swift`, `QwavaApp.swift`, `MixView.swift`, `FiltersView.swift`, `QuestionPlayerView.swift`, `NaturalReachView.swift`, `DailyQuestion.swift`, `TableModeAttributes/Session/LiveActivity`, `QwavaWidget.swift`, `WatchStore/SettingsView/QuestionView`, `QwavaWatchWidget.swift`, `Localizable.xcstrings` (grew to 399 keys)

**Unresolved**
- DE proofreading on device (own chat); Live Activity intent titles still EN; Android needs the whole German layer
