# Dev Log — July 2026

---

## 2026-07-04 — iOS: Deployment targets lowered to iOS 18.0 / watchOS 11.0, widget switched to static Inter fonts (Bold question)

### Deployment targets (Option A: one clean, consistent floor)
- Old state was inconsistent Xcode auto bumps: the app inherited iOS 18.5, QwavaWidgetExtension was 26.5, the watch app 26.2, the watch widget 26.5.
- New floor set deliberately and consistently: iOS 18.0 for the app and the widget, watchOS 11.0 for the watch app and the watch widget. Test targets aligned to match.
- Rationale: as of June 2026 iOS 26 is about 79 percent of iPhones and iOS 18 about 14 percent, so an 18.0 floor covers the large majority while letting the promoted widget reach iOS 18 users, which the 26.5 widget floor had been silently blocking. Decided against an iOS 17 fallback: small and shrinking share, not worth the extra availability guards and testing.
- Full scan of all 75 app Swift files plus the widget and watch confirmed nothing uses an iOS 26 or watchOS 26 API, so the move was a Build Settings change with no code changes. The only availability checks in the whole codebase are two `if #available(iOS 18.0, *)` blocks in SpaceQuestionDetailView.swift (the zoom transition); at an 18.0 floor they always take the iOS 18 path, so they were left untouched.

### Widget font: root cause and decision
- Symptom: the daily question rendered heavier in the simulator than on the real phone, and even the letter spacing differed between the two.
- First read was the known variable axis issue: the widget used the Inter variable font with the wght axis applied through kCTFontVariationAttribute, which the simulator honors but the device ignores.
- The deeper cause, found on device, was that the widget was not resolving its font at all and was falling back to the system font, which explains both the wrong weight and the wrong spacing. Different letter advance widths were the tell that it was a different font, not just a different weight.
- The fix has two parts that BOTH have to be true for a widget extension: the font files must be members of the QwavaWidgetExtension target (Copy Bundle Resources), and they must be registered in the widget's OWN Info.plist under UIAppFonts. The widget Info.plist had been missing the registration, so `UIFont(name:)` failed on device and fell back.
- Switched QwavaWidget.swift from the variable axis helper to a plain static face lookup by name. Added Inter-Regular.ttf, Inter-SemiBold.ttf, and Inter-Bold.ttf to the widget target and registered all three in QwavaWidget Info.plist.
- Decision: the widget question text is Inter-Bold, chosen for immediate glanceability on a busy home screen. SemiBold (600) was too subtle a step above Regular, especially as white text on the colored card. Inter-SemiBold.ttf stays in the project and registered, kept as an option if we revisit.

### Files changed
- QwavaWidget/QwavaWidget.swift: variable axis helper replaced with a static face lookup; question weight is Inter-Bold, label stays Inter-Regular. Paddings unchanged from Raf's latest.
- QwavaWidget/Info.plist: UIAppFonts now lists Inter-Regular.ttf, Inter-SemiBold.ttf, Inter-Bold.ttf.
- Qwava/Qwava/Fonts/: added static Inter-Regular.ttf, Inter-SemiBold.ttf, Inter-Bold.ttf, all members of the widget target.
- Xcode project: deployment targets set to iOS 18.0 (app, widget) and watchOS 11.0 (watch app, watch widget), test targets aligned.

### Open / follow up
- The app itself still routes all typography through the same variable font wght axis path (BrandFont in Theme.swift, via interVariable in InterVariableSupport.swift), so on device the app headings likely render at the base regular weight instead of their intended weights. Needs the same treatment as the widget: ship static Inter weights, select by name, keep the Dynamic Type scaling. Its own focused pass.
- Lesson for any future custom font in an extension: add the files to that extension's target AND register them in that extension's own Info.plist; do not rely on the variable font weight axis on device.

### Notes
- Not yet resubmitted to the App Store; these changes are committed but a new build has not been shipped. Raf will test the Bold widget across a few days with different daily questions before deciding anything further.

## 2026-07-04: Your Space question cards reworked, shadow transition lag fixed, decks list grouped, app page headers moved to Inter

### Favorites deck backs: question count
- Added the total question count line to the back face of favorited deck cards, matching what the Home carousel already showed. FavoritesView.makeCard now computes count via QuestionStore.shared.questions(forDeckKey:).count and passes it as PromoCard.questionCount; the card view already rendered the line when the field is set.

### Question text weight
- Investigated moving the question text from Medium to SemiBold across the player, the Your Space detail view, and the Your Space cards. Raf tried it and reversed it; question text stays Medium. No change shipped.

### Mini deck card as the "from deck" control
- New SpaceDeckMiniCard: a small horizontal deck card (image left, title and subtitle right) whose height is driven by its own text (12 top, title, subtitle, 12 bottom); the image fills the remaining height using a Color.clear plus overlay(image) plus fixedSize pattern so the text drives the row height. Image width 80. Corners and the 4px inset match the deck cards.
- Replaced the "from {deckName}" text at the bottom of SpaceBigQuestionCard and SpaceSmallQuestionCard with this element, wired to the existing jump to deck action (onTapFrom). Placeholder cards keep the old text for a later pass.
- Data: added deckCard: PromoCard? to both card structs. MyQwavaView resolves it via a new deckCard(forKey:) helper. Added a shared spaceDeckCard(forKey:) free function in SpaceCardHelpers.swift (reuses spacePromoCard(for:)) so the daily and sent pages can resolve the same way.
- Applied on Your Space, the daily archive page, the sent out lately page, and the full screen question detail view (SpaceQuestionDetailView), where the "from" button became the mini deck pinned to the bottom via a bottom safeAreaInset, matching where the player skip sits. Made SpaceDeckMiniCard internal (was fileprivate) so other files can use it.
- Font sizes: started with the big card using headline and callout and the small card using callout and caption. Later unified so both Your Space cards use the small pair (callout title, caption subtitle); the big pair (headline and callout, the horizontal deck card sizes) now appears only on SpaceQuestionDetailView. This removed the only 20pt on the page.

### Share square as the "Send to someone else" control
- New SpaceShareSquare: a square button to the right of the mini deck, side driven by the mini deck height (aspectRatio 1 plus fixedSize(horizontal: true)), deck background and outline colors, same 24 corner, a white square.and.arrow.up glyph (stays white in dark mode since deck colors are fixed). 4px gap via HStack(spacing: 4). Wired to onTapSend.
- Removing the text lifted the gap between the question and the bottom row to 32 on the big card and 24 on the small card. The glyph is nudged up with offset(y: -1) since an SF Symbol placed by font metrics reads slightly low.

### Shadow transition lag on minimize
- Symptom: the question card shadow appeared about a second after closing the full screen question with the pull down dismiss. Root cause: the shadow sat inside the view marked as the zoom transition source (.zoomSource), so SwiftUI suppressed it until the interactive dismiss settled. The drawing technique was never the cause; the location was.
- Fix: added a castsShadow flag (default true) to both question card structs. The Your Space, daily, and sent call sites pass castsShadow: false and apply .spaceCardShadow() after .zoomSource(...), outside the source, so it paints immediately. Deck cards and placeholders keep the internal shadow via the default.
- Made spaceCardShadow() internal (was a fileprivate extension) so SpaceSentView and SpaceDailyArchiveView can call it.
- SpaceSentView then hit "compiler unable to type check in reasonable time"; split its two cards into bigCard and smallCard ViewBuilder helpers to lift them out of body.
- Reworked the shadow shape into a short blurred bar pinned to the bottom (height 24, blur 8, pinned bottom, offset y 10) instead of a full card sized rectangle behind the card. The full size rectangle only stayed clean at the top while hosted inside the card; applied outside the source its bounds no longer hid behind the card, so the blur leaked on top and sides. The bottom bar cannot leak upward regardless of host.

### Decks list (SpaceDecksView)
- Fixed the "In deck order" sort: deck numbers are stored as "#01" through "#50", so Int(number) was nil and every deck tied at .max. Now Int(number filtered to digits only), so the digits drive the order.
- Most played sort grouped into five buckets by play count: On repeat (10+), Often (6 to 9), A few times (3 to 5), A couple of times (2), Just once (1). The two lightest buckets render as the compact SpaceHorizontalDeckCard; the rest stay big. Empty buckets hide, matching the recency grouping. Made SpaceHorizontalDeckCard internal so the decks page can use it.
- Recently played sort: split the single "This week" into Today, Yesterday, and Some days ago (Calendar isDateInToday and isDateInYesterday), keeping Last week, "2 to 4 weeks ago", and "Over a month ago".

### App page headers: system font to Inter
- Discovery and correction of an earlier note: the large page titles and their subtitles across the app were rendering in San Francisco, not Inter, because they used SwiftUI system text styles (.largeTitle.weight(.light) and .subheadline) rather than BrandFont. The app's own Inter path does render distinct weights on Raf's device, so the earlier worry that app headings render at a base weight did not hold; the real cause of non Inter headers was these system text styles. Every .font(.system(size:)) usage is on an SF Symbol and correctly stays system.
- Swapped .largeTitle.weight(.light) to BrandFont.h1(weight: .light) (both 34pt) and .subheadline to BrandFont.callout(weight: .regular) across MainTabsView, MixView, FavoritesView, MyQwavaView, MoreView, QuestionsView, and SpaceDetailScaffold. NaturalReachView "Share" title to BrandFont.h1(weight: .regular). SkippedSummaryVie: .title3.semibold to headline.semibold, .body to body, .callout to callout. CreateView .title2 to h3.
- Not touched: nav bar titles set via .navigationTitle (UIKit appearance, mostly empty or hidden), and Text views with no explicit font that fall back to system. Flagged as a possible later sweep.

### Your Space typography and spacing, carousel clip
- Greeting: experimented with size and weight for the time greeting and the name; both on h1 for now, Raf still tuning. Section headers on h3. Several top paddings adjusted by Raf.
- The "Where you left off" horizontal ScrollView was clipping the card shadow flat at the bottom. Recommended raising the scroll content bottom padding so the shadow fades inside the bounds; scrollClipDisabled() offered as the alternative that overflows on all edges (iOS 17 and up).

### Files changed
- Qwava/Qwava/Screens/Favorites/FavoritesView.swift
- Qwava/Qwava/Screens/MyQwava/MyQwavaView.swift
- Qwava/Qwava/Screens/MyQwava/SpaceCardHelpers.swift
- Qwava/Qwava/Screens/MyQwava/SpaceQuestionDetailView.swift
- Qwava/Qwava/Screens/MyQwava/SpaceDailyArchiveView.swift
- Qwava/Qwava/Screens/MyQwava/SpaceSentView.swift
- Qwava/Qwava/Screens/MyQwava/SpaceDecksView.swift
- Qwava/Qwava/Main/MainTabsView.swift
- Qwava/Qwava/Screens/Mix/MixView.swift
- Qwava/Qwava/Screens/More/MoreView.swift
- Qwava/Qwava/Screens/Questions/QuestionsView.swift
- Qwava/Qwava/Screens/MyQwava/SpaceDetailScaffold.swift
- Qwava/Qwava/Screens/Share/NaturalReachView.swift
- Qwava/Qwava/Screens/Content/SkippedSummaryVie.swift
- Qwava/Qwava/Screens/Filters/CreateView.swift

### Open / follow up
- Apply the new question card treatment (mini deck card, share square, single mini deck size, external shadow) to the placeholder question cards on Your Space; deferred to the content full pass.
- Add a suggested question for the user after the "Sent out lately" section on Your Space.
- Add a discovery carousel at the end of Your Space showing decks the user has not opened yet.
- Possible later sweep: Text with no explicit font, and nav bar titles, still render in the system font.

## 2026-07-05: Your Space placeholder question cards, lighter placeholder shadows, suggested question section, still to explore carousel and page, deck card sizing pass

A UI and functionality session on the Your Space tab. All Swift written here, pasted into Xcode manually, committed and pushed by Raf at the end. This session closed the three open Your Space follow ups from the July 4 session (placeholder question cards, a suggested question, a discovery carousel).

### Placeholder question cards get the mini deck and share square
- The placeholder branches of SpaceBigQuestionCard and SpaceSmallQuestionCard still showed the old "from DeckName" text plus a dimmed "Send to someone else?" text, left over from before the July 4 rework. They now render the same mini deck card plus square share button as the real cards, but inert.
- SpaceDeckMiniCard and SpaceShareSquare each gained an isPlaceholder flag. In placeholder mode the fill becomes PlaceholderDeckBg, the outline PlaceholderInk, and on the share square the glyph becomes PlaceholderInk instead of white, so the row reads greyed and inert. The placeholder path does not wrap the mini deck or the share square in buttons, so nothing is tappable.
- SpacePlaceholders.swift: QuestionPlaceholder gained fromSubtitle and imageName, and a new questionDeckCard(_:) helper builds a placeholder PromoCard for the mini deck (title is the fake deck name, subtitle a short tagline, cover one of placeholder_qwava_01 to 03, all text in neutralText). SpaceShareSlot gained a deckCard field so the sent placeholder slots carry their mini deck card.
- Placeholders keep the internal shadow (castsShadow default true), since they are never a zoom source and never had the lag that pushed the real cards to an external shadow.

### Lighter shadow for placeholder cards
- On the muted placeholder tones the shared 0.15 shadow read heavy. spaceCardShadow gained an opacity parameter defaulting to 0.15, and every placeholder capable card (the three deck cards and the two question cards) now passes opacity: isPlaceholder ? 0.05 : 0.15. Real cards keep 0.15 through the default, so only placeholders lightened.

### Suggested question section, "From a deck you like"
- New file SuggestedQuestionEngine.swift (Data/, Qwava target). Pure logic mirroring RecommendationEngine. Picks one question to send from a deck the user likes: favorited decks first (rotated per day for variety), then most played, taking the first deck that still has a question not in the recent sent history. Day stable pick. Returns a Suggestion { deck, question } or nil.
- MyQwavaView: a suggestedQuestion computed property feeds a new "From a deck you like" section placed right after Sent out lately, rendered as a SpaceSmallQuestionCard with the deck's real mini deck and share square, tap to expand full screen, share into Natural Reach. suggestionShare(_:) builds the SharedQuestion payload. Because sending records it, the question then appears in Sent out lately and is excluded from the suggestion next time, so the loop closes cleanly. The section hides when nil (no favorites and no plays).
- Decision: framed honestly as "from a deck you like", not a personalized pick. The person trigger idea (a prompt to think of who to send it to) and a discovery "worth a try" variant are parked for later, layered on top of this.

### Still to explore carousel and see more page
- New section at the bottom of Your Space: a horizontal row of up to six unplayed decks (never crossed a qualified play), mirroring Where you left off. Gated to appear only once at least one deck has been played and something is still unplayed. Day stable shuffle via a small FNV 1a hash seeded by the day, so the six rotate across days but hold within a day, cycling the whole catalog over time.
- MyQwavaView helpers: unplayedDecks, exploreStripDecks (capped 6), daySeed, exploreHash.
- SpaceDecksView: DeckSort gained a stillToExplore case (page title and menu label "Still to explore"), an unplayedDecks property (deck number order), and a content branch that renders the unplayed list with its own empty state ("You have played every deck. Nice."). The carousel See more opens the decks page on this sort. Note it is a different set (the unplayed decks) rather than a reorder of the played set, and the branch runs before the played empty gate.
- Decision: strictly never played, kept separate from the "not played in a while" revisit story, which stays with the Home labels and the recommendation revisit rule. Good future hook for promoting new decks, which are unplayed for everyone.

### Deck card sizing pass
- Mini deck vertical padding moved from 12 to 10, parameterized as verticalPadding on SpaceDeckMiniCard (default 10). The full screen detail view passes verticalPadding: 12 to keep its larger mini deck unchanged.
- Share square made a true square. The old aspectRatio(1) plus fixedSize(horizontal: true) could pin the width to the collapsed ideal while the height stretched, reading taller than wide. New MiniDeckRow wraps the mini deck and the share square, measures the mini deck's rendered height with a GeometryReader and a preference key (MiniDeckHeightKey), and pins the square to that on both sides. All four question card call sites (big and small, real and placeholder) route through MiniDeckRow. The share glyph nudge moved from -1 to -2 for the now correct, slightly larger square.
- Big and horizontal deck cards: 8px gap between the subtitle and the deck number (was 2).
- Horizontal deck card made self sizing to its text. Removed the fixed height (which had briefly gone from 136 to 120, then to 158 while chasing the number) and the trailing Spacer, and switched to the mini deck fill pattern (Color.clear at width 154, maxHeight infinity, image as an overlay) with fixedSize vertical, so the card height is top 16, title, subtitle, 8 gap, deck number, bottom 16, landing around 122 to 128 and staying correct under Dynamic Type. This component is shared by On heavy rotation, the recommended deck, and the most played buckets on the decks page, so all pick up the tighter self sizing card with no call site changes.
- Vertical deck card made more vertical: the cover turned portrait (was 158 by 154, now 150 by 180 after width tuning), the card width went 166 to 158, the subtitle now allows two lines with reservesSpace so the carousel rows stay even, and the subtitle to number gap rose to 12.

### Files
- New: SuggestedQuestionEngine.swift (Qwava/Qwava/Data/)
- Changed: MyQwavaView.swift, SpacePlaceholders.swift, SpaceDecksView.swift, SpaceQuestionDetailView.swift

### Open / follow up
- Android parity for this whole session: placeholder question cards (mini deck plus share square), lighter placeholder shadows, the suggested question section, the still to explore carousel and page, the deck card sizing pass, and the true square share.
- The suggested question and still to explore sections have no placeholder state; they simply hide when empty. A placeholder pass could give brand new users the full page shape later if wanted.

## 2026-07-05: Your Space parity with the two July iOS sessions, plus a Quick Mix subtitle fix

Brought Android up to iOS on everything the July 4 and July 5 sessions changed, all in the Your Space tab. All Kotlin, pasted into Android Studio by Raf, committed and pushed by Raf at the end.

### Question cards: mini deck plus square share
- SpaceCards.kt: added SpaceDeckMiniCard, SpaceShareSquare, and MiniDeckRow. Both question cards (SpaceBigQuestionCard, SpaceSmallQuestionCard) gained a deckCard: DeckCard? parameter and now render the mini deck plus square share row instead of the old "from DeckName" text and "Send to someone else?" text. When deckCard is null the card just pads the bottom, matching iOS.
- The square share uses Icons.Outlined.Share (Android's existing share glyph) nudged up 2dp, deck colors, white glyph that stays white in dark mode. MiniDeckRow uses Row height(IntrinsicSize.Min) with the square as fillMaxHeight().aspectRatio(1f, matchHeightConstraintsFirst = true), so it stays a true 1:1 without the GeometryReader/preference-key approach iOS needed.
- spaceCardShadow gained an opacity parameter (default 0.15f); placeholders pass 0.05f so the muted tones do not read heavy. Real cards unchanged.

### Placeholders
- SpacePlaceholders.kt: QuestionPlaceholder gained fromSubtitle and imageName, and a questionDeckCard(q, ink, bg) helper builds a placeholder DeckCard for the mini deck. The three sent placeholders carry a greyed, inert mini deck plus square share via the isPlaceholder flag.

### From a deck you like, Still to explore
- New SuggestedQuestionEngine.kt (Data/), pure logic mirroring the iOS engine: favorited decks first (rotated per day), then most played, first deck that still has an unsent question, day stable pick. Returns a Suggestion(deck, question) or null.
- MyQwavaScreen.kt: added the "From a deck you like" section after Sent out lately (a small question card, hides when null), and the "Still to explore" carousel at the bottom (up to six unplayed decks, day stable FNV 1a shuffle seeded by the day, gated on hasPlayedDecks and something still unplayed). Its See more opens the decks page on a new still_to_explore sort. New helpers: deckCardFor, unplayedDecks, exploreStripDecks.

### Decks list
- SpaceDecksScreen.kt: added the STILL_TO_EXPLORE sort (unplayed decks in deck number order, empty state "You have played every deck. Nice.", evaluated before the played empty gate). Most played now groups into five bands (On repeat 10+, Often 6 to 9, A few times 3 to 5, A couple of times 2, Just once 1) with the two lightest rendered as the compact SpaceHorizontalDeckCard. Recently played split into Today, Yesterday, Some days ago, then Last week, 2 to 4 weeks ago, Over a month ago. Deck number sort fixed with a shared Deck.numberValue() that filters digits, since numbers are stored like "#01".

### Deck card sizing pass
- SpaceHorizontalDeckCard made self sizing via Row height(IntrinsicSize.Min) and a matchParentSize cover, removing the fixed 136 height. SpaceVerticalDeckCard made more vertical: width 166 to 158, cover 158x154 to 150x180 portrait, subtitle now two lines, subtitle to number gap 8 to 12. Big and horizontal deck cards: gap above the deck number 2 to 8. Mini deck default verticalPadding 10; the detail view passes 12.

### Other call sites
- SpaceSentScreen.kt and SpaceDailyArchiveScreen.kt pass deckCard into their question cards via a new deckCardFor helper.
- SpaceQuestionDetailScreen.kt restructured: the question now centers in a weighted area and a larger mini deck (headline title, callout subtitle, verticalPadding 12) is pinned to the bottom, replacing the "from DeckName" text and running the jump confirm on tap. Share stays in the toolbar.

### Quick Mix subtitle fix
- QuickMixScreen.kt: subtitle was "A custom set of questions, instantly.", inconsistent with iOS and the feature reference. Changed to "Your questions, instantly."

### Deliberate divergences from iOS
- Did not port the iOS shadow lag rework (the external shadow, the castsShadow flag, the bottom bar shadow shape). That existed only because the iOS zoom transition suppressed the shadow during the interactive dismiss. Android has no such transition, so it keeps its internal full rect card shadow; only the opacity parameter was added for lighter placeholders.
- Did not port the iOS system font to Inter header fix. Android headers already run through BrandFont (Inter), so there was nothing to correct.

### Files changed
- SpaceCards.kt, SpacePlaceholders.kt, MyQwavaScreen.kt, SpaceDecksScreen.kt, SpaceSentScreen.kt, SpaceDailyArchiveScreen.kt, SpaceQuestionDetailScreen.kt, QuickMixScreen.kt
- New: SuggestedQuestionEngine.kt (app/src/main/java/com/qwava/app/data/)

### Open / follow up
- The From a deck you like and Still to explore sections have no placeholder state on Android either; they simply hide when empty, matching iOS.

## 2026-07-06: Text ink token system, Your Space deck labels, lighter card shadow, native micro animations and haptics

### Text ink tokens
- New assets in Colors/Text: `TitleInk` (heading ink) and `SublineInk` (softer subline), light and dark, not pure black/white.
- Rule: all titles TitleInk, all sublines SublineInk, ignoring tab background. Brand primary (PrimaryDark) reserved for interactive text, links, and header utility icons (shuffle, filter, settings). Text on colored deck cards untouched.
- Sublines callout (14pt) to body (17pt).
- Applied to all five tab headers. On More, one `headerColor` let repointed to TitleInk (also carries the section titles); subline overridden to SublineInk.
- Your Space: new `headerSubline` (time greeting plus name when signed in with a name, else "Your decks, your stats."); separate body greeting block removed. Section headers and two descriptive labels to TitleInk; dead `textColor` removed.
- Subpages: `SpaceDetailScaffold` title, `SpaceDecksView` sectionHeader, `SpaceSentView`/`SpaceDailyArchiveView` day labels to TitleInk.
- Overlays: `FiltersView` SectionHeader to TitleInk (shared with the Depth sheet, so covers Audience, Closeness, Category, Depth). Favorites and Your Space settings section headers to TitleInk; clear buttons stay brand primary.

### Your Space deck labels on big cards
- `promoCard(for:)` and `spacePromoCard(for:)` resolve the active label via `PersonalizationEngine.activeEntry` + `DeckLabelsStore`, mirroring Home.
- `SpaceBigDeckCard` draws a capsule badge above the description in the deck's label colors; description capped 3 lines to 2. Only the big card.

### Lighter Your Space card shadow
- Real card shadow opacity 0.15 to 0.12 (`spaceCardShadow` default plus the `isPlaceholder ? 0.05 : 0.15` ternary). Placeholders stay 0.05.

### Sizing
- Mini deck vertical padding 10 to 8. Big question card text h2 medium (28pt) to h3 medium (22pt).

### Favorites carousel alignment
- Removed an extra `.padding(.bottom, 12)` on the Favorites header inset so The Keepers carousel lines up with Home.

### Micro animations and haptics (native)
- Carousel swipe: `onChange(of: centeredID)` fires `Haptics.playImpact(Haptics.soft)`, guarded against load and shuffle.
- Save heart: `.contentTransition(.symbolEffect(.replace))` plus `.symbolEffect(.bounce, value:)`.
- Live counts (Quick Mix, Filters): `.contentTransition(.numericText())` with a scoped `.animation`.
- Bookmark: replace plus bounce on `isFavorite`.
- Quick Mix segmented controls: `Haptics.playSelection()` in each picker setter, guarded.
- Deck cover press: new `PressableCardStyle` scaling to 0.97 (Raf trying 0.95) on press; carousel card wrapped in a Button.

### Files changed
- Assets Colors/Text; MainTabsView, MixView, FavoritesView, MyQwavaView, MoreView; SpaceDetailScaffold, SpaceDecksView, SpaceSentView, SpaceDailyArchiveView, SpaceCardHelpers; FiltersView, FavoritesSettingsView, YourSpaceSettingsView, QuestionPlayerView, CardCarouselView

### Open
- Port the deck cover press to the Your Space cards (own components, not CardCarouselView).
- Android parity for this session.

## 2026-07-07: Your Space card press, entry screen ink and copy, watch widget rework, question widow fix

### Press across Your Space cards
- Made `PressableCardStyle` reusable (removed `private`).
- Deck cards: each Your Space deck card call site wrapped in a Button using it, disabled on placeholders.
- Question cards: `SpaceBigQuestionCard` and `SpaceSmallQuestionCard` wrapped in a Button for the expand tap (shadow inside the label so it scales), disabled on placeholders; MiniDeckRow stays inner buttons.

### Micro animations and haptics
- Home shuffle icon spins a full turn on shuffle: `.rotationEffect(.degrees(Double(shuffleNonce) * 360))` with a spring.
- Sharing sheet: selection tick on the recency and closeness `.onChange` handlers.

### Entry screens: ink and copy
- Welcome: "Qwava" to TitleInk, tagline to SublineInk; tagline copy "Your pocket icebreaker" to "Good talks made easy".
- Login (AuthView): headline to TitleInk, subline to SublineInk.
- Set name: headline to TitleInk, subline to SublineInk.

### Copy
- Your Space daily section renamed "Your daily question" to "Today's question".
- "See more" kept uniform across sections.

### h1 weight experiment (reverted)
- Tried all page titles h1 light to regular; Raf disliked it, reverted. Titles stay light.

### Question widow fix
- New `String+Typography.swift`, `noLastLineOrphan` replaces the last space with a non breaking space so the last word is not left alone. Applied to the four Your Space question card Texts, SpaceQuestionDetailView, QuestionPlayerView.

### Depth sheet title
- QuestionPlayerView has its own duplicate `SectionHeader`; title PrimaryDark to TitleInk so Depth matches the other overlays. Two duplicate SectionHeader structs exist.
- Tried mirroring FiltersView toolbar modifiers to lighten the Depth Reset/Apply glass; no effect, reverted (native iOS 26 toolbar glass).

### Favorites empty placeholder
- Placeholder title, subtitle, description colors derive from the outline hex via `colorFrom(outlineHex)`.

### Watch widget rework
- Text to `.body`, `.lineLimit(3)`, `.minimumScaleFactor(0.7)`, background `Color("PrimaryExtraDark")`. Key three-line fix: removed the manual `.padding(8)`, which cost a line in the accessoryRectangular slot.
- PrimaryExtraDark recreated in the watch widget's own asset catalog (single universal value; main app `#9C1B3C` / `#E34F73`).

### Files changed
- CardCarouselView, MainTabsView, MyQwavaView, NaturalReachView; WelcomeView, AuthView, SetDisplayNameView; SpaceQuestionDetailView, QuestionPlayerView, FavoritesView; String+Typography.swift (new); QwavaWatchWidget

### Open
- Watch widget still needs a real Smart Stack test (simulator pairing blocker).
- Android parity for this session. OnboardingView not yet checked for stray brand primary titles.

## 2026-07-07: Widow rule refinement, header icon alignment, Favorites shuffle spin

### Widow rule refined
- `noLastLineOrphan` now glues short function words (a, an, the, to, of, and so on) to the next word, and only pulls the last word up when short (4 letters or fewer, ignoring punctuation), leaving long final words alone. SwiftUI Text has no text-wrap balance.

### Header icon alignment
- Header icon container top padding 2 to 4 on Home, Favorites, Your Space so icons sit level with the title. Quick Mix and More have none.

### Favorites shuffle spin
- Added the shuffle spin (`.rotationEffect` on `shuffleNonce * 360` plus spring) to Favorites to match Home.

### Files changed
- String+Typography.swift, MainTabsView, FavoritesView, MyQwavaView

## 2026-07-09: Android ink token system, tab headers, Your Space header greeting (Android)

### Sync issue caught first
- GitHub main was stuck at 2026-06-27: the whole 2026-07-05 Android session had been applied locally but never pushed. Raf pushed it before continuing. Reminder: the app-code commit and push is Raf's step, confirm it lands.

### Text ink tokens
- New `TitleInk` (#191407 / #FEFFFF) and `SublineInk` (#302C20 / #FBFBFB) matching iOS, in Color.kt, wired into QwavaColors + Light/DarkQwavaColors in Theme.kt.
- Rule: titles titleInk, sublines sublineInk, brand primary only for interactive text, links, header utility icons.

### Tab headers
- All five: title titleInk, subline sublineInk, subline style callout to body.

### Your Space header
- Folded the time-of-day greeting into the header subline (`headerSubline`); removed the separate greeting block. Daily section renamed "Your daily question" to "Today's question".

### Quick Mix subtitle
- Fixed to "Your questions, instantly." (the 2026-07-05 fix had never reached main).

### Files changed
- Color.kt, Theme.kt, HomeScreen.kt, QuickMixScreen.kt, FavoritesScreen.kt, MyQwavaScreen.kt, MoreScreen.kt

## 2026-07-10: Android ink sweep completion, visual parity, micro animations and haptics (Android)

### Ink sweep, remaining surfaces
- Your Space subpages (section header helper, recommendation reason and "From a deck you like" labels, subpage titles, Sent and Daily archive day labels) to titleInk.
- Overlays: Filters and Depth section labels to titleInk; overlay top-bar titles ("Filters", "Depth Filter") primaryDark to titleInk. (First read the iOS ".primary" note as brand pink; .primary is the system dark label.)
- Settings sheets: Favorites and Your Space settings titles and the Clear data header to titleInk; clear buttons stay pink.

### Entry screens, aligned to iOS
- Welcome: "Qwava" and tagline to titleInk, tagline "Good talks made easy".
- Auth: headline titleInk (two-line break), subline sublineInk; kept Android's sign-in vs sign-up subline variants.
- Set name: headline titleInk ("What should we call you?"), subline sublineInk, whole screen left-aligned to match login (headline h2 to displayL).

### Deck label badges on big cards
- New `Deck.toDeckCardWithLabel(...)` in DeckCard.kt mirroring Home; SpaceBigDeckCard draws the badge above the description (capped 3 to 2). Only the big card, on Your Space rotation and the decks pages.

### Sizing and shadow
- Real card shadow 0.15 to 0.12 (placeholders 0.05). Mini deck padding 10 to 8 (detail view still 12). Big question card text h2 to h3.

### Question widow fix
- New `StringTypography.kt`, `noLastLineOrphan` ported from iOS (glue set includes "my", excludes "you"/"your"). Applied to the question card body, expanded detail, player.

### Micro animations (Compose)
- Shuffle spin (Home, Favorites): `animateFloatAsState` on `shuffleNonce * 360`, graphicsLayer rotationZ.
- Press dip to 0.97 with a spring: Home carousel card, the three Your Space deck cards and both question cards (shared `pressableCard` helper), the expanded-detail mini deck.
- Drag to dismiss on the expanded question: ported the player's dragDown gesture onto the Scaffold; painted the app background so the reveal is white; top bar fades.
- Rolling counts (Quick Mix, Filters): `AnimatedContent`, vertical slide plus fade.
- Bookmark and save-heart pop: single keyframes 1 to 1.2 over 220ms (chained springs held at the peak). PlayerIconButton got an optional scale param.
- Player question transition: `AnimatedContent` keyed on index, horizontal slide plus fade (leaves the vertical axis for drag to dismiss).
- Haptics: carousel `HapticsHelper.click` on `pagerState.currentPage` change (mid-swipe), drop(1) on load; NaturalReach segmented controls tick on a real switch.

### Android-only touch
- On shuffle the carousel `animateScrollToPage(0)` sweeps back to the first deck, ticking through the ones it passes. Intentional Android difference.

### Files changed
- SpaceCards.kt, DeckCard.kt, MyQwavaScreen.kt, SpaceDecksScreen.kt, SpaceSentScreen.kt, SpaceDailyArchiveScreen.kt, FiltersScreen.kt, DepthFilterScreen.kt, FavoritesSettingsSheet.kt, YourSpaceSettingsScreen.kt, WelcomeScreen.kt, AuthScreen.kt, SetDisplayNameScreen.kt, HomeScreen.kt, FavoritesScreen.kt, DeckCardView.kt, QuestionPlayerScreen.kt, SpaceQuestionDetailScreen.kt, QuickMixScreen.kt, NaturalReachScreen.kt, StringTypography.kt (new)

### Open
- Not yet released; all July 9 and 10 Android work is local plus GitHub only.
- "From a deck you like" and "Still to explore" still hide when empty.
- iOS " AuthView.swift" leading-space filename resolved 2026-07-11 (Raf renamed it).

## 2026-07-11: iOS v1.2.4 submitted to the App Store, mini deck press on the full page question

### Code
- `SpaceQuestionDetailView`: the big mini deck at the bottom of the full page question was on `.buttonStyle(.plain)`; swapped to `.buttonStyle(PressableCardStyle())` so it presses like the cards.
- Marketing version bumped to 1.2.4 (app, widget, watch).
- Raf made small onboarding and deck content tweaks.

### App Store Connect
- Completed Apple's new age rating questionnaire (social media capability questions, required from September 2026). Qwava has no social feed, UGC distribution, user messaging, profiles, follows, livestreaming, or ads, so all No; rating stays 4+.
- What's New written, build attached, submitted for review. 1.2.4 carries the whole July polish pass.

### Open
- Reconsider the App Store category (Social Networking to Lifestyle); can affect ranking and Search Ads.
- Copyright year in App Store Connect still 2025; bump to 2026.

## 2026-07-11: Android favorite decks deckKey fix and v1.2.3 release (Android)

### What changed
- Favorite decks now key by `deckKey` in the deck card and the player, matching iOS and the Favorites tab.
- Previously the card and player used `imageName` while the tab and iOS use `deckKey`, so decks favorited on iOS and synced in showed empty bookmarks even though they appeared in the tab. Four one-line edits in `DeckCardView.kt` and `QuestionPlayerScreen.kt`.
- Bumped `versionCode` to 6 and `versionName` to 1.2.3.
- Refreshed the bundled `decks.json` and `decksfav.json`, added `android_onboarding04.png`.
- Built the signed AAB and submitted Android v1.2.3 to Google Play; waiting for review.

### Key decisions
- `deckKey` is canonical for favorite decks: iOS keys by deckKey and cloud sync carries deckKeys, so the card and player were the outliers, not the tab.
- No migration for old `imageName` entries already in a user's stored set; they become harmless orphans and iOS-synced deckKeys are unaffected.

### Files changed
- `DeckCardView.kt`, `QuestionPlayerScreen.kt`, `build.gradle.kts`, `decks.json`, `decksfav.json`, `android_onboarding04.png`

### Release
- Android v1.2.3 (versionCode 6) submitted to Google Play, waiting for review. Carries the July 9 and 10 Android polish (ink sweep, entry copy, deck label badges, shadows and sizing, widow fix, micro animations, haptics) plus this favorites fix.

### Unresolved
- Google Play review pending.

## 2026-07-12: iPad universal build, width adaptive type and layouts

### What changed
- Enabled iPad: `TARGETED_DEVICE_FAMILY` set to `1,2`, added `INFOPLIST_KEY_UISupportedInterfaceOrientations_iPad` (portrait, upside down, both landscape). App was iPhone only, so it ran in compatibility mode on iPad before.
- `BrandFont` gains a static `deviceScale`, multiplied into the shared `inter(size:weight:relativeTo:)` funnel and the four adaptive functions. `wideScale` currently `1.06`. Set from `horizontalSizeClass` in `RootView.body` (regular means wide), so onboarding, welcome, and tabs all scale from the first frame. iPhone stays `1.0`.
- Carousel `CardCarouselView` wide path: card width is a fixed `wideCardWidth` (`320`) capped by `maxCardWidth`; height from width via `cardAspect` (`1.61`, the iPhone proportion) capped by `maxCardHeight`. iPhone path (`geo.width * 0.82`, screen height clamp) unchanged.
- Carousel wide alignment: `wideEdgeInset` (`20`) leading padding, `defaultScrollAnchor` and `scrollPosition` anchored `.leading` when wide (center on iPhone), so the first deck lines up with the title and there is no empty left space.
- Dropped the peek scale on wide: all cards render full size, even spacing, no corner fly in. Removed the `onScrollVisibilityChange` and `visibleIDs` path; wide uses `scaleEffect(1.0)`, iPhone keeps the centered `scaleEffect(0.96)`.
- CTA buttons `Buttons+CTA.swift`: new `CTAWidth` modifier caps full width buttons to a shared `wideMaxWidth` (`452`) on iPad, centered; iPhone stays `.infinity`. Applied to primary, secondary, tertiary.
- Onboarding centered columns on iPad: `WelcomeView` illustration becomes a contained panel (`welcomeWideWidth` `560`, `welcomeWideHeight` `480`) instead of edge to edge, which fixes the landscape band from the full bleed `ParallaxContainer`. `AuthView` and `SetDisplayNameView` cap their content column to `500` and center (`authMaxWidth`, `nameMaxWidth`); fields land at `452` to match the button.
- `QuestionPlayerView`: question, note, and the end screen texts capped to `playerTextWidth` (`640`) reading measure on iPad, precomputed as a `playerMaxWidth` local in `body`. Buttons already cap via the shared rule.
- Launch screen `LaunchScreen.storyboard`: logo was pinned top left by its autoresizing mask; switched to real constraints, centered, width `0.55` of the screen capped at `320`.

### Key decisions
- Width signal is `horizontalSizeClass` (regular is wide) for the global type scale, not a measured width breakpoint; measured width is reserved for per screen grid density later.
- Deck card text stays on the global scale (kept scaling together); widened the card instead of exempting individual card texts, to avoid mixed scaling within one card.
- One shared iPad button width across the whole app (`wideMaxWidth` `452`), matching the auth column (`500` minus `24` padding each side), rather than per screen widths.
- Carousel leading aligned when wide, not centered, to kill the empty left space; the trailing card crop is accepted.
- `playerMaxWidth` lifted to a local in `body`; five width ternaries inline had pushed the type checker over its time limit.
- Launch screen cache is stubborn: needs the app deleted and reinstalled, or the Simulator erased, to render again.

### Files changed
- `project.pbxproj`, `Theme.swift`, `RootView.swift`, `CardCarouselView.swift`, `Buttons+CTA.swift`, `AuthView.swift`, `SetDisplayNameView.swift`, `QuestionPlayerView.swift`, `LaunchScreen.storyboard`

### Release
- iOS v1.2.4 (App Store) and Android v1.2.3 (Google Play) both approved and live, resolving the pending review items from 2026-07-11.

### Unresolved
- iPad Home is still a single carousel row floating in an otherwise empty screen; a grid for the wide layout is the real fix, deferred.
- Screens not yet adapted for wide: Your Space, decks, Favorites, filters, Quick Mix, plus the wide sidebar navigation option.
- A very narrow iPad Split View column still uses the phone height path in the carousel (`UIScreen.main.bounds.height`).
- Native iPad tab bar (iPadOS 26 Liquid Glass) background is not themeable; only the tint is controllable.
- Tuning values are current picks and may still move: `wideScale` `1.06`, `wideCardWidth` `320`, `wideMaxWidth` `452`, `authMaxWidth` and `nameMaxWidth` `500`, `playerTextWidth` `640`, launch logo `0.55`.
- iPad build not yet submitted; work in progress.

## 2026-07-13 — iPad: Quick Mix and More width caps, Your Space Today's question and On heavy rotation carousels

### What changed
- Quick Mix `MixView`: card container capped to `500` with `24` inner padding on iPad, so the full width Create button lands at `452`, matching the auth column; iPhone stays `420` / `16`, button full width. Added `hSizeClass`.
- More `MoreView`: content column capped to `500` and centered on iPad, iPhone full width; added `hSizeClass`. Kept the custom card layout, not a `Form`; header not aligned to the column.
- Your Space `MyQwavaView`: added `hSizeClass` and iPad width constants `wideQuestionCardWidth` `380`, `wideBigDeckCardWidth` `440`.
- Today's question, iPad: horizontal carousel of `DailyQuestion.recent(days: 5)`, newest first, the same source as the See all archive, reusing `SpaceBigQuestionCard`; iPhone keeps the single card.
- `SpaceBigQuestionCard`: new `cardWidth` param. Set (iPad) reserves three lines via `lineLimit(3)` over a hidden `Q\nQ\nQ` sizer so cards match height, keeping the iPhone `40` top and `32` gap paddings. `nil` renders as before.
- On heavy rotation, iPad: horizontal carousel of three `SpaceBigDeckCard` from `rotationSlots`, each at `wideBigDeckCardWidth`; iPhone keeps one big plus two small.
- `SpaceBigDeckCard`: new `cardWidth` param; `nil` keeps full width.

### Key decisions
- Width signal is `horizontalSizeClass == .regular` for every iPad branch, as on July 12.
- Quick Mix and More use a capped centered column (`500`, the auth form mechanism); Your Space uses full width carousels instead, since it is a dashboard not a form.
- Your Space carousels run full width, not capped centered blocks, so shelves read as one family; this also set On heavy rotation as a carousel rather than three stacked capped cards.
- Question card reserves three lines via a hidden sizer, not a fixed height, so it is exactly a three line question tall; the `40`/`32` paddings move iPhone and iPad together by design.
- Big deck card wider than the question card (`440` vs `380`) because it is a horizontal image plus text card; a shared grid width across shelves is not a goal.
- Home wide grid dropped by Raf; the July 12 "grid is the real fix" plan is off, Home wide layout has no chosen solution.

### Files changed
- `Qwava/Qwava/Screens/Mix/MixView.swift`
- `Qwava/Qwava/Screens/More/MoreView.swift`
- `Qwava/Qwava/Screens/MyQwava/MyQwavaView.swift`

### Unresolved
- Your Space still to adapt for iPad: Where you left off and Still to explore (wider `SpaceVerticalDeckCard`, count up to ~10), Sent out lately (carousel of big question cards reusing the daily card), Recommended and From a deck you like (left align, cap width).
- On the wide layout, section headers do not line up with capped or centered content (Quick Mix, More, Your Space capped sections); a shared header treatment across screens is deferred.
- iPad tuning numbers provisional: `wideQuestionCardWidth` `380`, `wideBigDeckCardWidth` `440`, plus the July 12 picks.
- iPad build not submitted; continues the July 12 work in progress.
- Still open from July 12: Favorites and filters not adapted, the wide sidebar navigation option, the narrow Split View column on the phone height path, the non themeable iPadOS 26 tab bar background.

## 2026-07-14 — iPad: Your Space deck strips, Sent out lately carousel, Recommended and suggested card widths, placeholder counts

### What changed
- `MyQwavaView`: added iPad constants `wideVerticalDeckCardWidth` `210` and `stripCount` (`9` regular, `6` compact), `sentCount` (`5` regular, `3` compact).
- Where you left off and Still to explore, iPad: wider `SpaceVerticalDeckCard` and up to `9` items; `recentDecks`, `exploreStripDecks` and `leftOffSlots` now take `stripCount`.
- `SpaceVerticalDeckCard`: new `cardWidth` param; cover derived from it (`imageW` = `cardW - 8`, `imageH` = `imageW * 1.2`) so it scales in proportion. `nil` keeps the iPhone `158` / `150x180`.
- Sent out lately, iPad: horizontal carousel of big question cards at `wideQuestionCardWidth` via new `sentCarousel`; iPhone keeps one big plus two small. `recentShares` and `sentSlots` take `sentCount`.
- `SpaceHorizontalDeckCard`: new `cardWidth` param; Recommended passes `wideBigDeckCardWidth` on iPad so it matches the heavy rotation card. `nil` keeps full width.
- `SpaceSmallQuestionCard`: new `cardWidth` param; From a deck you like passes `wideQuestionCardWidth` on iPad. `nil` keeps full width.
- `SpacePlaceholders`: three deck placeholders added reusing covers `01` to `03`; `leftOff` now `prefix(9)`, `recommendationCard` now `decks.last!` so it stays out of the strip. Two question placeholders added (`ph-q-4`, `ph-q-5`), `questions` now `5`.

### Key decisions
- `stripCount` `9` not `10`: a strip that stops short pushes the user to See all; `9` fills the 13 inch.
- Recommended reuses `wideBigDeckCardWidth` rather than its own constant, so it tracks the heavy rotation cards from one number.
- Recommendation placeholder moved to the end of `decks` and read via `last`, so no cover repeats between the strip and the recommendation once `leftOff` grew to `9`.
- Placeholder decks reuse existing covers rather than adding art; placeholders only show in the empty first run.
- Question card stays vertically centered; a top aligned variant was tried and reverted.
- Card widths differ per shelf by design (`380` question, `440` deck, `210` strip); a shared grid width is not a goal.

### Files changed
- `Qwava/Qwava/Screens/MyQwava/MyQwavaView.swift`
- `Qwava/Qwava/Screens/MyQwava/SpacePlaceholders.swift`

### Unresolved
- `SpaceQuestionDetailView` not adapted for iPad: cap the question to the `QuestionPlayerView` reading measure, cap the mini deck row to the shared CTA width `452`. Started but not written; file was not fetched.
- iPad tuning numbers provisional: `wideVerticalDeckCardWidth` `210`, `wideQuestionCardWidth` `380`, `wideBigDeckCardWidth` `440`, `stripCount` `9`, `sentCount` `5`.
- Home wide layout has no chosen solution; the July 12 grid plan was dropped.
- Section headers still do not line up with capped or centered content on the wide layout; shared header treatment deferred.
- iPad build not submitted; Favorites and filters not adapted, wide sidebar navigation option open.

## 2026-07-15: iPad see all grids, question detail caps, carousel sizing rule, mini deck square crop

### What changed
- `Theme.swift`: `wideScale` `1.06` to `1.0`; iPad type is now 1:1 with iPhone.
- `SpaceBigQuestionCard`: top padding `40` to `32`, gap to the mini deck `32` to `24`; both platforms.
- `Buttons+CTA.swift`: `CTAWidth` made internal so `wideMaxWidth` `452` can be referenced outside the file.
- `SpaceQuestionDetailView`: question capped to `playerTextWidth` `640` on iPad, mini deck capped to `CTAWidth.wideMaxWidth` and centered; width locals lifted out of `body` with `return`.
- `QuestionPlayerView`: `.frame(maxWidth: .infinity)` added before `.contentShape(Rectangle())`. The July 12 `640` cap on the question `Text` had made it the widest child, collapsing the VStack and with it the tap to advance area and the background.
- `SpaceCardHelpers.swift`: new `SpaceCardGrid<Item, ID, Card>`. `LazyVGrid` with `GridItem(.adaptive(minimum: minCardWidth))` on regular, plain `VStack` on compact. `minCardWidth` `380`, `spacing` param, own `horizontalInset` (`20` regular, `16` compact).
- `SpaceDecksView`: all four sort branches on `SpaceCardGrid`. Per card `.padding(.horizontal, 16)` in the recent and most played branches moved to the grid, since padding inside a cell doubles the gutters.
- `SpaceSentView`, `SpaceDailyArchiveView`: rebuilt on `SpaceCardGrid`, all `SpaceSmallQuestionCard`. The big first card is gone from both, on iPhone too.
- `SpaceSmallQuestionCard`: new `reservedLines: Int?`. Hidden `Q\nQ\nQ` sizer in a `ZStack`, no `lineLimit`. `nil` on compact keeps today's self sizing.
- `RemoteDeckImage`: new `small: Bool`. Remote URL and bundled fallback both follow it via `assetName`. `SpaceDeckMiniCard` passes `small: true`.
- `CardCarouselView`: compact card height now `min(cardWidth * cardAspect, maxCardHeight)`, same rule the wide path already used. `UIScreen.main.bounds.height`, `heightFraction` and `minCardHeight` deleted. `maxCardWidth` `360` now applied to the compact path, where it was always meant to be; on the wide path `min(320, 360)` never bound.
- `DepthFilterSheet`: unused `hSizeClass` and `playerTextWidth` deleted, pasted in on July 12 and never referenced.

### Key decisions
- `wideScale` `1.0`: the width caps now do the adaptation, Apple does not scale body type between iPhone and iPad, and a blanket multiplier compounds with Dynamic Type.
- Grid column count comes from available width via `.adaptive(minimum:)`, never orientation. An iPad in Split View can be any width, so orientation is not a proxy for room; also means no breakpoints and no GeometryReader.
- See all pages use only the small card. The big card is a highlight slot Raf wants to invest in later; a see all page is reference. Removes the mixed row height problem and the special first cell in one move.
- `reservedLines` is a floor, never a cap. Measured `questions.json`: 3,220 questions, avg 70 chars, max 110. That one 110 char question is four lines in a small card on a standard iPhone, so `lineLimit(3)` would truncate it; a floor gives even rows and lets it grow.
- `_small` is a different crop, not a downscale: `1501x1501` square against `2161x3241` (`0.667`) for the full file. Ratios decided the call: the big deck card slot is `154x228` (`0.675`), a near exact match for the full artwork, so it keeps it. Mini deck is `80x42` and keeps only ~35% of the portrait against ~52% of the square.
- `_small` restricted to the mini deck by Raf; the horizontal deck card (`154x~120`, keeps ~51%) stays on the full file, since `_small` is cut for genuinely small slots.
- `SpaceBigDeckCard` needs no `reservesSpace`: it is `.frame(height: 236)`, so descriptions never move it. Reserving would bottom anchor a two line box and float a one line description a line above the padding.
- Carousel: on iPhone `width * 0.82` and `screenHeight * 0.61` agree only because a phone's width and height are locked together. Modelled the divergence: landscape Split View gave `0.93`, portrait Split View `0.41`, against the intended `0.62`. Aspect rule returns `0.62` everywhere; iPhone 15 moves `0.1pt`, SE `5pt`.
- Grid inset `20` on iPad to line up with section headers, which sit at `20`. Empty state texts left at `16`; they belong to the deferred shared header pass.

### Files changed
- `Qwava/Qwava/DesignSystem/Theme.swift`
- `Qwava/Qwava/DesignSystem/Buttons+CTA.swift`
- `Qwava/Qwava/Components/CardCarouselView.swift`
- `Qwava/Qwava/Screens/Content/QuestionPlayerView.swift`
- `Qwava/Qwava/Screens/MyQwava/SpaceCardHelpers.swift`
- `Qwava/Qwava/Screens/MyQwava/SpaceDecksView.swift`
- `Qwava/Qwava/Screens/MyQwava/SpaceSentView.swift`
- `Qwava/Qwava/Screens/MyQwava/SpaceDailyArchiveView.swift`
- `Qwava/Qwava/Screens/MyQwava/SpaceQuestionDetailView.swift`
- `Qwava/Qwava/Screens/MyQwava/MyQwavaView.swift`

### Open bug: share sheet dismisses itself, unresolved
- `NaturalReachView` appears and disappears under a second, nothing tapped in between.
- Broken: Favorites > deck > question > share. Your Space > question card > full screen question > share. Your Space > question > open original deck > share, which also closes the player. On iPhone the same routes make "Open original deck" do nothing at all.
- Works: Home > deck > question > share. Quick Mix > question > share.
- Key fact: Favorites opens the same `QuestionPlayerView` with the same deck and the same share button as Home. Only the presenter differs, so the fault is in the presentation context, not the player, the detail view or `NaturalReachView`.
- Ruled out, do not retest: the zoom transition (`.zoomDestination` removed, no change); presentation modifier order (detail view reordered to match the player, no change); `safeAreaInset` (both use it); `@State` reset (disproved, instance id constant across the flicker via a console probe); `NaturalReachView` self dismissing (`showShareSheet` only set by the Continue button); `fullScreenCover` attached inside a `NavigationStack` (moved the Favorites cover to the body root to match `MainTabsView` and `MixView`, no change, reverted).
- Unexplained: `onDisappear` and `onAppear` fire together at the moment the sheet vanishes, not when it appears, and state survives. Delay is ~1s, so something asynchronous is involved rather than the tap.
- Next: what `MainTabsView` and `MixView` share that `FavoritesView` and `MyQwavaView` do not. Both broken screens stack a `.sheet` and a `.fullScreenCover`; the working ones do not. `FlatNavBar` sets UIKit nav bar appearance and is applied differently across them; unverified.
- Not a release blocker: the Your Space card share squares still work, and Home and Quick Mix are the main play paths.

### Unresolved
- Remote refresh has been failing silently: `DecodingError.keyNotFound` for `ok` in `ContentManifest`. `manifest.json` has no `ok` field. Questions still load from another path, but the manifest refresh has been dead.
- `RemoteSmallCover` in `QuestionPlayerView` builds the images URL itself, so it ignores the `imagesBaseURL` override from the manifest, and has no bundled fallback; a failed load leaves `Color.clear`.
- Placeholder covers have no `_small` twin (all nine are `placeholder_*`, `1441x2161`, `0.667`). The prefix branch keeps them on the full file, so a placeholder mini deck crops differently to a real one on Your Space. Deferred pending a look on device; nine imagesets carry light and dark at 2x and 3x, so ~36 exports.
- `CardCarouselView` compact path in a narrow Split View column now sizes correctly, but Home's wide layout still has no chosen solution.
- Section headers still do not line up with capped or centered content on Quick Mix and More; shared header pass deferred.
- Filters need no iPad work: both filter UIs present as form sheets, which report compact, so the iPhone layout renders at a sensible width. Any `.regular` branch inside a sheet is dead by construction. `.presentationDetents([.medium])` on the depth sheet unverified on iPad.
- Favorites needs no iPad work either: it renders the shared `CardCarouselView` and inherited the July 12 adaptations.
- iPad tuning numbers: `minCardWidth` `380`, `wideScale` `1.0` not yet judged on device; the July 12 to 14 picks stand.
- iPad build not submitted; iPad screenshots and the wide sidebar option still open.

## 2026-07-16

### What changed
- Share sheet bug fixed: `FavoritesView` and `MyQwavaView` no longer present the player or the question detail themselves.
- `FavoritesView` takes `playingCard` as a `@Binding` from `MainTabsView`; its own `.fullScreenCover` deleted. `fullScreenPlayer(for:)` gained a Keepers branch, so one cover serves Home, Favorites and The Keepers.
- `MyQwavaView` takes `expandedQuestion` as a `@Binding` and `zoomNS` as a `Namespace.ID`; that cover moved to `myQwavaTab()`. `#Preview` removed, since the view now takes arguments.
- Completed screen in `QuestionPlayerView` takes the deck's `cardBackgroundColor`, with `cardOutlineColor` on the cover, the Close button and the bookmark chip, and `subtitleColor` on the subline. Headline white and `regular`.
- New `hiddenByDepthCount` names how many questions the depth filter is holding back; `restart()` now replays `depthFilteredQuestions` instead of the whole deck.
- `BrandFont.deviceScale` and `wideScale` deleted, with the `RootView` writer and five call sites.
- `ContentManifest.ok` now `Bool?`, guarded in `RemoteContentManager.dict(from:)`. `manifest.json` on the CDN gained `"ok": true`.

### Key decisions
- The share bug was never a regression: `TARGETED_DEVICE_FAMILY` went `1` to `"1,2"` on 2026-07-12, so the app only started running on iPad then, where `.sheet` presents as a form sheet. Always in the code, unreachable on iPhone.
- Fixed by copying Home's structure rather than diagnosing further: Home's cover sits on the `TabView` with nothing else on the chain, both broken screens stacked `.sheet` and `.fullScreenCover` on one view two `NavigationStack`s deep.
- `expandedQuestion`'s cover went on `myQwavaTab()`, not the `TabView`: a second cover there would rebuild the stacking that caused the bug.
- `deviceScale` deleted rather than kept as a dial: `wideScale` was `1.0`, so every multiplication was a no-op while reading as live, and `RootView` was writing a global from inside `body`.
- Completed screen colour comes from `DeckStore` by `deckID`, so `quick_mix` and `favorite01` fall back to the system background; neither is in `DeckStore`.
- Manifest fixed on the CDN first: it repairs every installed copy including v1.2.4 with no review. `ok: Bool?` is defensive only and ships with the next build.

### Files changed
- `Qwava/Qwava/Main/MainTabsView.swift`
- `Qwava/Qwava/Screens/Favorites/FavoritesView.swift`
- `Qwava/Qwava/Screens/MyQwava/MyQwavaView.swift`
- `Qwava/Qwava/Screens/Content/QuestionPlayerView.swift`
- `Qwava/Qwava/App/RootView.swift`
- `Qwava/Qwava/DesignSystem/Theme.swift`
- `Qwava/Qwava/Data/RemoteContent.swift`
- `Qwava/Qwava/Data/RemoteContentManager.swift`

### Unresolved
- The completed screen X keeps the system toolbar chrome; an outline ring on top read badly and was removed, only the background fill survived.
- `DepthFilterSheet` offers depths a deck has no questions at: `Between Meetings` is 80 `light` questions behind three toggles. Selecting Medium empties `depthFilteredQuestions` and the fallback silently serves the whole deck.
- Depth defaults are light-heavy: 49 of 50 decks hide something by default, `Weekend Debrief` and `Networking Unplugged` hide 50 of 100 each. Content decision, now surfaced by the new line.
- `RemoteSmallCover` still builds its own images URL, so it ignores `images_base_url`, which is live for the first time since the manifest fix.
- Hex parser now duplicated three times, file private in `FavoritesView`, `MainTabsView` and `QuestionPlayerView`.
- Placeholder covers still have no `_small` twin.

## 2026-07-16: iOS parity catch up, then the tablet layout (Android)

### Parity catch up
- `SpaceCards.kt`: big question card top padding `40` to `32`, mini deck gap `32` to `24`. The iOS July 15 change applied to both platforms; Android had missed it.
- `SpaceDeckCover` gained `small: Boolean`, `SpaceDeckMiniCard` passes `small = true`. Remote URL and bundled fallback both follow one `assetName`. All 50 decks plus `favorite01` already shipped `_small`; only the wiring was missing.
- `SpaceSentScreen`, `SpaceDailyArchiveScreen`: big first card gone, all `SpaceSmallQuestionCard`, phone included.
- `QuestionPlayerScreen`: completed screen takes the deck's `cardBackgroundColor`, with `cardOutlineColor` on the cover, Close button and bookmark chip, and `subtitleColor` on the new depth line. Headline `MEDIUM` to `LIGHT`, matching iOS.
- New `hiddenByDepthCount`; headline swaps to "That's this round of X" when non zero. `PlayerIconButton` gained an optional `tint` so the Close X can go white on a coloured screen.
- Share glyph `.offset(y = -2)` removed. Ported from iOS, where SF Symbol `square.and.arrow.up` hangs low in its box; Material's `Share` is already centred, so the lift only pushed it high.
- `QuestionsScreen.kt` deleted: dead since Your Space replaced the Questions tab, and carrying a third copy of the carousel.

### Tablet: foundation
- New `WindowSize.kt`: `isWideWindow` (`screenWidthDp >= 600`), `Modifier.wideWidth(max)`, `Modifier.wideCenteredWidth(max)`, `FORM_MAX_WIDTH` `500`, `CTA_MAX_WIDTH` `452`, `READING_MAX_WIDTH` `640`.
- New `DeckCarousel.kt`, extracted from `HomeScreen` and `FavoritesScreen`, which ran identical carousel code with different click handlers. Wide path: `wideCardWidth` `320` capped at `maxCardWidth` `360`, height `min(available, width * cardAspect 1.61)`, leading `wideEdgeInset` `20`, peek scale dropped.
- New `SpaceCardGrid.kt`: column on a phone, measured grid on a tablet. `gridMinCardWidth` `380`, inset `20` regular / `16` compact.

### Tablet: screens
- Entry: `AuthScreen` and `SetDisplayNameScreen` columns and the `WelcomeScreen` button column at `FORM_MAX_WIDTH`, centred; welcome illustration capped at `560`.
- `QuestionPlayerScreen`: question and completed column at `READING_MAX_WIDTH`, Next and Close at `CTA_MAX_WIDTH`.
- `QuickMixScreen`: card at `FORM_MAX_WIDTH`, screen margin dropped and inner padding `16` to `24` on wide, so Create lands at `452`.
- `MoreScreen`, `FiltersScreen`, `DepthFilterScreen`, `YourSpaceSettingsScreen`: content column at `FORM_MAX_WIDTH`, centred.
- `NaturalReachScreen`: content column at `FORM_MAX_WIDTH`, Continue to share at `CTA_MAX_WIDTH`; the bar behind it stays full width.
- Your Space: Today's question and Sent out lately become `LazyRow`s of `SpaceBigQuestionCard` at `wideQuestionCardWidth` `380`; On heavy rotation a `LazyRow` of three `SpaceBigDeckCard` at `wideBigDeckCardWidth` `440`; strips take `wideVerticalDeckCardWidth` `210` and `stripCount` `9`; `sentCount` `5`; Recommended and From a deck you like reuse the shelf widths.
- All four deck and question cards gained `cardWidth: Dp?`; `null` reproduces the phone exactly. `SpaceVerticalDeckCard` derives its cover from it (`imageW = cardW - 8`, `imageH = imageW * 1.2`).
- See all pages on `SpaceCardGrid`: `SpaceSentScreen`, `SpaceDailyArchiveScreen`, all four `SpaceDecksScreen` sort branches.

### Key decisions
- No gate to open, unlike iOS. `targetSdk` `36` means Android 16 ignores orientation and resizability restrictions at `sw600dp`, so v1.2.3 already runs full screen on every tablet. This fixed something live rather than opening a door.
- Width signal is `screenWidthDp >= 600`, reading the window not the screen, so a narrow split screen renders the phone layout. No new dependency; `material3-window-size-class` answers the same question.
- Grid column count comes from available width, never orientation: a tablet window can be any width in split screen.
- `SpaceCardGrid` is not a `LazyVerticalGrid`. Both see all pages sit inside a `verticalScroll`, where a nested lazy vertical crashes at measure. Tens of cards, so eager costs nothing.
- Carousel aspect rule gated to wide only. iOS unified both paths on July 15 and the iPhone moved `0.1pt`, but Android's compact path is `maxHeight * 0.76`, a different formula, so unifying would visibly shift the phone.
- Carousel extracted before adapting: Android had three copies where iOS had one shared view.
- `reservedLines` is a floor on a grid cell; `cardWidth` caps at three lines on a carousel card. A fixed width card must not grow, a grid cell may.
- Type scale untouched. iOS settled on `1.0` and deleted the dial, so there was nothing to port.
- `FavoritesSettingsSheet` needs nothing: Material3 caps a `ModalBottomSheet` at `640` on large screens.
- Filters do need work here, unlike iOS, where both filter UIs present as form sheets and report compact. On Android they are full nav destinations.
- No `ok` fix needed. Gson never threw on the missing `ContentManifest.ok`, so Android's remote refresh has worked the whole time; the CDN's new `"ok": true` is inert.

### Files changed
- New: `WindowSize.kt`, `DeckCarousel.kt`, `SpaceCardGrid.kt`. Deleted: `QuestionsScreen.kt`.
- `SpaceCards.kt`, `MyQwavaScreen.kt`, `HomeScreen.kt`, `FavoritesScreen.kt`, `QuestionPlayerScreen.kt`, `QuickMixScreen.kt`, `MoreScreen.kt`, `WelcomeScreen.kt`, `AuthScreen.kt`, `SetDisplayNameScreen.kt`, `FiltersScreen.kt`, `DepthFilterScreen.kt`, `YourSpaceSettingsScreen.kt`, `NaturalReachScreen.kt`, `SpaceSentScreen.kt`, `SpaceDailyArchiveScreen.kt`, `SpaceDecksScreen.kt`

### Unresolved
- `SpacePlaceholders` holds 7 decks and 3 questions against `stripCount` `9` and `sentCount` `5`, so a brand new tablet shows short rows. Needs three deck placeholders and two question placeholders written in the Decks chat, then `leftOff` to `take(9)` and `recommendationPlaceholder` to `last()`.
- Section headers stay pinned left while content centres on Quick Mix, More and Natural Reach. Same gap on iOS, parked on both.
- No shared CTA component: nine hand rolled buttons across eight files. `wideWidth` caps them, but nothing stops the next one drifting.
- Most played light bands use `SpaceHorizontalDeckCard`, which sizes to its content, so two in one tablet row can come out uneven. Unjudged on device.
- Every tablet number is an iOS pick, none judged on an Android tablet: `cardAspect` `1.61`, `wideCardWidth` `320`, welcome illustration `560`, `FORM_MAX_WIDTH` `500`, `CTA_MAX_WIDTH` `452`, `READING_MAX_WIDTH` `640`, `380` / `440` / `210`, `stripCount` `9`, `sentCount` `5`, `gridMinCardWidth` `380`.
- `RemoteImageConfig.baseUrl` is written from the manifest and never read; every image URL is hardcoded. Same gap as iOS's `RemoteSmallCover`. `RemoteContentManager.fetchManifest` is dead code.
- Not released. Tablet screenshots for the Play listing still to do.
